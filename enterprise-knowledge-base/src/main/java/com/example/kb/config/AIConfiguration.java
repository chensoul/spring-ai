package com.example.kb.config;

import org.springframework.context.annotation.Configuration;

/**
 * AI配置类（简化版本）
 * 
 * 注意：这是一个简化版本，用于演示基本的Spring Boot应用结构
 * 完整的Spring AI集成需要相应的依赖和配置
 */
@Configuration
public class AIConfiguration {
    
    // 简化版本，暂时移除Spring AI相关配置
    // 在实际部署时，可以添加Spring AI依赖和相关配置
    
}